name: Terraform Plan

on:
  pull_request:
    paths:
      - 'terraform-skeleton/**'
      - '.github/workflows/terraform-*.yml'

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  plan:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        env: [mut, lt, st, prod]

    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.10.x"

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets[format('AWS_ACCOUNT_{0}', matrix.env)] }}:role/coupon-${{ matrix.env }}-cicd-plan-role
          aws-region: ap-northeast-1

      - name: Terraform Format Check
        run: terraform -chdir=terraform-skeleton/environments/${{ matrix.env }} fmt -check -recursive
        continue-on-error: true

      - name: Terraform Init
        run: terraform -chdir=terraform-skeleton/environments/${{ matrix.env }} init

      - name: Terraform Validate
        run: terraform -chdir=terraform-skeleton/environments/${{ matrix.env }} validate

      - name: tflint
        uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: latest

      - name: Run tflint
        run: |
          cd terraform-skeleton
          tflint --init
          tflint --recursive

      - name: tfsec
        uses: aquasecurity/tfsec-action@v1.0.3
        with:
          working_directory: terraform-skeleton
          soft_fail: false

      - name: Checkov
        uses: bridgecrewio/checkov-action@master
        with:
          directory: terraform-skeleton/environments/${{ matrix.env }}
          framework: terraform
          soft_fail: false

      - name: Terraform Plan
        id: plan
        run: |
          terraform -chdir=terraform-skeleton/environments/${{ matrix.env }} plan \
            -out=tfplan \
            -no-color \
            -input=false

      - name: Upload Plan Artifact
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ matrix.env }}-${{ github.run_id }}
          path: terraform-skeleton/environments/${{ matrix.env }}/tfplan
          retention-days: 30
