apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-app-config
  labels:
    app: demo-app
data:
  # アプリケーション設定
  APP_NAME: "K8s Demo App"
  APP_VERSION: "1.0.0"
  ENVIRONMENT: "production"
  PORT: "3000"

  # ログレベル
  LOG_LEVEL: "info"

  # その他の設定
  NODE_ENV: "production"
