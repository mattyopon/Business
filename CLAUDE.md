# Business リポジトリ作業ガイド

## 図解作成ルール (全案件共通、2026-05-20 追加)

**本リポジトリ内の文書に図を挿入する際は、必ず GenSpark CLI (`gsk img`) で生成する**。ASCII アート図 / Mermaid / draw.io 手動作成を新規作成しない。

### 基本ルール (詳細はグローバル `~/.claude/CLAUDE.md` § 図解作成ルール)
1. **対象**: アーキ図 / フロー図 / シーケンス図 / マトリクス / タイムライン / 状態遷移 / マインドマップ / 組織図 / ワークフロー / ガント等 **全種類**
2. **ツール**: `@genspark/cli` (`gsk img`、デフォルト nano-banana-2)
3. **出力**: 案件内 `figures/` ディレクトリに `NN_descriptive_name.png` 形式 (連番)
4. **挿入**: Markdown は `![alt](../figures/NN_name.png)` で相対パス
5. **価値選別なし**: 「これは図にする価値があるか」を勝手に判断しない
6. **AWS 公式アイコン**: AWS アーキ図はプロンプトに「Use AWS official architecture icons」必須
7. **比率**: デフォルト `-r "16:9"`
8. **並列生成**: 複数図は `gsk img ... &` でバックグラウンド + `wait` で同期

### 既存案件への適用例
- `ANK-未割当-金融クーポン管理/figures/` : 37 図生成済、各章 Markdown に挿入済
- 詳細運用: `ANK-未割当-金融クーポン管理/PM向け/09_変更影響範囲ルール.md § H-bis`

---

## 新規案件作成時の自動タスク

新規案件（ANK番号）の準備を依頼された場合、以下を**自動的に**実行すること：

### 1. 基本ディレクトリ構成
```
ANK{番号}/
├── README.md
├── 商談用/
│   ├── 商談準備資料.md      # スキルシート
│   ├── 技術用語解説.md      # 技術用語の解説
│   ├── 想定Q&A.md          # 想定される質問と回答
│   ├── ヒアリングシート.md   # 商談時確認項目
│   └── 資料/
│       ├── README.md
│       └── GenSpark_プロンプト.md  # ← 必ず作成（git外）
├── 参画用/
│   └── 運用ドキュメント/
└── 技術検証/
```

### 2. GenSparkプロンプトの自動作成

**必須**: 商談用/資料/GenSpark_プロンプト.md を必ず作成する

#### プロンプトテンプレート構成
1. **提案資料作成プロンプト** - PowerPoint形式の提案資料
2. **システム構成図プロンプト** - AWS/クラウド構成図
3. **セキュリティ構成図プロンプト** - 多層防御設計図
4. **CI/CDパイプライン図プロンプト** - デプロイフロー
5. **監視ダッシュボード構成プロンプト** - 監視設計

#### プロンプト作成のポイント
- 案件の技術スタック（AWS, GCP, Azure等）に合わせる
- 具体的なサービス名・構成を明記
- コスト概算を含める
- スライド構成（10-15枚程度）を指定
- AWS公式カラー・アイコン使用を指示

### 3. .gitignore への追加

GenSparkプロンプトは内部作業用のため、以下を.gitignoreに追加：
```
ANK{番号}/商談用/資料/GenSpark_プロンプト.md
```

### 4. クライアント情報の取り扱い

- 実際のクライアント社名はREADME等に記載しない
- `（非公開）`または`（サンプル企業名）`と記載

## 参考テンプレート

既存案件のGenSparkプロンプトを参考にする：
- ANK0000639199/商談用/GenSpark構成図プロンプト.md（医療IT/MLOps）
- ANK0000640318/商談用/資料/GenSpark_プロンプト.md（金融決済）
- ANK0000642825/商談用/資料/GenSpark_プロンプト.md（検索システム）

---

## 一時ファイル管理ルール

### 基本方針

長時間の調査・分析タスクでは、コンテキストの圧迫を防ぐため**一時ファイル（scratchpad）を積極的に活用**すること。

### 運用ルール

1. **調査結果の保存**
   - 大量のデータを扱う調査は、結果を一時ファイルにまとめる
   - ファイル名は内容がわかる命名にする（例: `company_research.md`, `api_comparison.md`）
   - 調査が進むにつれて情報を更新・統合する

2. **ファイルの統合**
   - 同じテーマの複数ファイルは1つに統合する
   - 古いバージョンは統合後に削除する
   - 例: `company_research.md` + `company_benefits.md` → `company_research_complete.md`

3. **定期的なクリーンアップ**
   - セッション終了時に不要な一時ファイルを削除
   - 重複・冗長なファイルを整理
   - 最終成果物のみを残す

4. **ファイル構成例**
   ```
   scratchpad/
   ├── [調査名]_complete.md    # 最終統合版
   └── [調査名]_detail.md      # 詳細データ（必要に応じて）
   ```

### 注意事項

- 一時ファイルはセッション固有のため、次回セッションでは利用できない場合がある
- 重要な調査結果はユーザーに提示してから保存する
- ファイルサイズが大きくなりすぎないよう、適宜分割する
